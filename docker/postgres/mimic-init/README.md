Place MIMIC-IV 3.1 SQL initialization scripts here.

Examples:
- 001_schema.sql
- 002_seed_data.sql

These files are executed automatically by the Postgres container on first startup only (when data volume is empty).
