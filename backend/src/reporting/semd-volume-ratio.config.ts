import type { SemdVolumeRatioConfig } from './semd-volume-ratio.calculator'

/**
 * Четыре показателя-доли, взятые в работу на ВКС 07.08.2026. Отличаются только данными,
 * поэтому и заданы данными: расчёт у всех один — `calculateSemdVolumeRatio`.
 *
 * Виды СЭМД в числителе — коды «Вид МД» (`reporting_semd_types.nsi_oid`), листы
 * знаменателя — `reporting_tpgg_plan_values.sheet_code` (число в начале имени листа:
 * «3.2 Дисп.в.н.» → `3.2`).
 *
 * Разметка листов взята из письменного ТЗ методолога от 07.08.2026, где по каждому
 * показателю листы перечислены поимённо. Она шире той, что выводилась из методики
 * самостоятельно, и это проверено на данных: у 6.1.3.2.9 добавились листы 3.6 и 3.7,
 * у 6.1.3.2.10 — 6, 7 и 9.
 *
 * Вопрос «входят ли подлисты 3.х в лист 3» снят самой разметкой: лист 3 стоит только
 * в знаменателе 6.1.3.2.8, подлисты 3.2–3.9 — только в 6.1.3.2.9. Двойного счёта нет.
 *
 * Идентификаторы — существующие строки `reporting_indicators`: эти четыре показателя
 * заведены ещё миграцией 0013 и до сих пор стояли в статусе «ожидаются данные»
 * (числитель и знаменатель пустые, план из «Приложения 2» загружен). Новые id заводить
 * нельзя — код показателя уникален, а плановые значения привязаны к старым строкам.
 */

/**
 * Вид 551 «Сведения о медицинском осмотре по репродуктивному здоровью».
 *
 * Методика от 10.07.2026 (слайд 32) включает его в числитель 6.1.3.2.9 в 2026 году
 * третьим слагаемым — к наибольшему из видов 340 и 141. Письменное ТЗ методолога
 * от 07.08 его не упоминает. В выгрузке РЭМД у вида ноль документов, поэтому сегодня
 * значение показателя от флага не зависит; разойтись с методикой он даст молча,
 * как только вид появится.
 *
 * Держим выключенным до ответа методолога (вопрос 9.4 ТЗ). Включение — правка одной
 * строки, миграций и переимпорта не требует, но требует пересчёта периода.
 */
export const SEMD_VOLUME_RATIO_INCLUDES_TYPE_551 = false

const REPRODUCTIVE_HEALTH_EXAM_CODES = SEMD_VOLUME_RATIO_INCLUDES_TYPE_551 ? ['551'] : []

export const SEMD_VOLUME_RATIO_CONFIGS: readonly SemdVolumeRatioConfig[] = [
    {
        indicatorId: 'semd_outpatient_epicrisis',
        code: '6.1.3.2.8',
        // «Эпикриз по законченному случаю амбулаторный» (OID 92, 233).
        semdTypeCodes: ['2'],
        aggregate: 'sum',
        // Случаи оказания ПМСП: обращения по заболеваниям и посещения.
        tpggSheetCodes: ['2', '3', '4'],
    },
    {
        indicatorId: 'semd_preventive_exam',
        code: '6.1.3.2.9',
        // «Результаты профилактического медицинского осмотра/диспансеризации» (OID 228).
        // Методика требует наибольшее из двух видов, а не их сумму, — это не опечатка.
        semdTypeCodes: ['340', '141'],
        aggregate: 'max',
        additionalSemdTypeCodes: REPRODUCTIVE_HEALTH_EXAM_CODES,
        // Диспансеризации и профилактические осмотры: 3.2–3.9 без листа 3.
        tpggSheetCodes: ['3.2', '3.3', '3.4', '3.5', '3.6', '3.7', '3.8', '3.9'],
    },
    {
        indicatorId: 'semd_inpatient_discharge',
        code: '6.1.3.2.10',
        // «Эпикриз в стационаре выписной» (OID 77, 147, 235) и (или) «Выписной эпикриз
        // из родильного дома» (OID 89, 205, 308) — здесь именно сумма двух видов.
        semdTypeCodes: ['1', '10'],
        aggregate: 'sum',
        // Круглосуточный и дневной стационары, ВМП, медицинская реабилитация.
        tpggSheetCodes: ['5', '6', '7', '8', '9'],
    },
    {
        indicatorId: 'semd_ambulance_call_card',
        code: '6.1.3.2.11',
        // «Карта вызова скорой медицинской помощи» (OID 115).
        semdTypeCodes: ['74'],
        aggregate: 'sum',
        tpggSheetCodes: ['1'],
    },
    {
        /**
         * Д-21, диспансерное наблюдение. Постановка методолога от 24.08.2026:
         * «давайте подумаем, как нам прикрутить фактическое количество случаев
         * к кривой количества СЭМДов».
         *
         * Знаменатель — итог листа 2.2 без разбивки по группам заболеваний:
         * файл фонда делит случаи на БСК, онкологию, сахарный диабет и прочие,
         * а вид СЭМД один и заболевания в нём не записано. Разложить документы
         * по группам нечем — решение Ильи от 27.08.
         */
        indicatorId: 'semd_dispensary_observation',
        code: 'ДН',
        // «Протокол консультации в рамках диспансерного наблюдения» (OID 111).
        semdTypeCodes: ['85'],
        aggregate: 'sum',
        tpggSheetCodes: ['2.2'],
    },
]

export function findSemdVolumeRatioConfig(
    indicatorId: string,
): SemdVolumeRatioConfig | null {
    return SEMD_VOLUME_RATIO_CONFIGS.find(
        (config) => config.indicatorId === indicatorId,
    ) ?? null
}
