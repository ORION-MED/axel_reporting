import type { Migration } from '../migration.types'

/**
 * Признак «участие МО в обеспечении граждан ЛЛО» — колонка, добавленная методологом
 * в справочник признаков МО 13.08.2026.
 *
 * Появилась под условие из заполненной формы на 145 видов: «реализация государственных
 * и региональных программ по обеспечению населения ЛЛО» (приоритет обязательности 4,
 * виды 37 «Льготный рецепт…» и 38). Без признака перечень МО для этого условия взять
 * неоткуда, и льготный рецепт оказался бы обязательным у всех МО подряд.
 *
 * Отдельной колонкой, а не в JSONB лицензий: это не лицензия, а участие в программе,
 * и условие в матрице ссылается на него именно как на именованный признак — так же,
 * как на прикреплённое население.
 *
 * Пустое значение эквивалентно прежнему поведению: признак не отмечен ни у кого,
 * условие не сработает ни для одной МО.
 */
export const migration: Migration = {
    id: 43,
    name: 'add_organization_llo_program',
    upSql: `
    ALTER TABLE reporting_organization_attributes
        ADD COLUMN IF NOT EXISTS llo_program BOOLEAN NOT NULL DEFAULT FALSE;
    `,
}
