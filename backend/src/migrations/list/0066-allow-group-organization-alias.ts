import type { Migration } from '../migration.types'

/**
 * Групповой синоним МО: одно наименование намеренно закреплено за несколькими
 * организациями.
 *
 * В матрице у видов 48, 50 и 357 в «Только эти МО» стоит одно слово «Санаторий»,
 * а организаций две — ГБУ «Санаторий "Озеро Горькое"» и ГБУ «Детский санаторий
 * "Космос"». Методолог подтвердила состав сообщением от 28.08.2026: «Забираем их —
 * они подведы ДЗО, и для них есть свои СЭМД».
 *
 * До этой миграции любое имя, за которым нашлось больше одной МО, считалось
 * неоднозначным и пропускалось — и правильно: «Диспансер» у вида 90 их четыре,
 * и молча взять все четыре значило бы придумать за методолога. Поэтому группа
 * не выводится из числа совпадений, а объявляется явно: `alias_kind = 'group'`.
 * Всё остальное по-прежнему неоднозначно.
 *
 * Сами строки синонимов здесь не заводятся. Внешний ключ на `reporting_organizations`
 * строгий, а санаториев в реестре нет до первой загрузки справочника признаков МО —
 * миграция на пустой базе вставила бы ноль строк и осталась бы отмеченной
 * выполненной. Список переехал в `organization-alias-seed.ts` и применяется
 * импортом справочника, когда организации уже созданы.
 */
export const migration: Migration = {
    id: 66,
    name: 'allow_group_organization_alias',
    upSql: `
    ALTER TABLE reporting_organization_aliases
        DROP CONSTRAINT IF EXISTS reporting_organization_aliases_kind_chk;

    ALTER TABLE reporting_organization_aliases
        ADD CONSTRAINT reporting_organization_aliases_kind_chk
        CHECK (alias_kind IN ('official_full', 'official_short', 'common', 'source', 'legacy', 'group'));
    `,
}
