import type { Migration } from '../migration.types'

/**
 * Р3, вариант «развязать уровни» (решение от 29.07.2026).
 *
 * `reporting_organization_semd_requirements.gis_available` — поле уровня пары МО × вид,
 * но фактически в него писалась **региональная** эвристика импортёра ТПГГ: «если вид
 * зарегистрирован хоть кем-то в регионе, значит региональная ГИС его умеет». Для региона,
 * закрывшего все 35 видов, это давало TRUE во всех 1 295 парах, из-за чего:
 *
 * 1. справочник МИАЦ (миграция 0037) не мог сработать вообще — он подключён как fallback
 *    через COALESCE, а слева всегда стояло TRUE;
 * 2. каждая причина «вид обязателен, но не формируется» попадала в зону ответственности МО,
 *    даже когда функциональность в региональной ГИС для этой МО не поднята.
 *
 * Обнуляем поле: других источников у него нет. Матрица применимости его не пишет
 * (в её INSERT/ON CONFLICT этого столбца нет), ручные уточнения хранятся в отдельной
 * таблице reporting_organization_semd_requirement_overrides и признака ГИС не содержат.
 * Проверено на рабочей базе: все заполненные значения имели metadata->>'source' =
 * 'applicability_matrix' и пришли из прежнего импорта ТПГГ.
 *
 * После миграции поле остаётся в схеме и снова заполняется только осознанным значением
 * по конкретной паре МО × вид. Региональные источники (справочник МИАЦ и факт РЭМД)
 * подключаются в расчёте явным каскадом, а не через запись в требования.
 */
export const migration: Migration = {
    id: 38,
    name: 'clear_requirement_regional_gis_heuristic',
    upSql: `
    UPDATE reporting_organization_semd_requirements
    SET gis_available = NULL
    WHERE gis_available IS NOT NULL;
    `,
}
