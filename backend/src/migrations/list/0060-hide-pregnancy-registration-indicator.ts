import type { Migration } from '../migration.types'

/**
 * Показатель 1.24 убирается с экрана — решение Ильи от 26.08.2026.
 *
 * Считается он верно: 16 зданий из 20, знаменатель из перечня Минздрава,
 * числитель по нашим выгрузкам РЭМД. Но **целевого значения у него нет** —
 * в «Приложение 2» он не входит, а методолог цели пока не назвала. Без цели
 * оценка «выполнено / ниже плана / критическое отклонение» не считается,
 * и на карте показатель даёт ровный серый: цифры есть, смысла нет.
 * Показывать такую соту рядом с шестью работающими — вводить в заблуждение.
 *
 * **Расчёт при этом не выключается.** `is_mvp` управляет только выдачей
 * показателей наружу (`reporting-values.service.ts`), а пересчёт вызывается
 * напрямую из `ReportingService`. Значения продолжат обновляться при каждом
 * пересчёте, и когда цель появится, вернуть показатель — одна строка
 * `is_mvp = TRUE`, без перезагрузки данных.
 *
 * Оговорка к описанию в миграции 0058: там сказано, что числитель берётся
 * из перечня, потому что здание в фактах РЭМД не сохранялось. С миграции 0059
 * это уже не так — здания сохраняются, и числитель считается по ним.
 */
export const migration: Migration = {
    id: 60,
    name: 'hide_pregnancy_registration_indicator',
    upSql: `
    UPDATE reporting_indicators
       SET is_mvp = FALSE,
           updated_at = now()
     WHERE id = 'semd_pregnancy_registration';
    `,
}
